---
import Layout from "@/layouts/Layout.astro";
import { type Service } from "@/types/Service";
import { type Image } from "@/types/Image";
import { type Review } from "@/types/Review";
import { type ListItemContact } from "@/types/ListItemContact";
import { services } from "@/const/Services";
import { imagesServices } from "@/const/ImagesServices";
import ServiceItem from "@/components/ServiceItem.astro";
import BentoImageItem from "@/components/BentoImageItem.astro";
import { reviewsList } from "@/const/Reviews";
import ReviewCard from "@/components/ReviewCard.astro";
import { ItemsContact } from "@/const/ItemsContact";
import ContactItem from "@/components/ContactItem.astro";

const images: Image[] = imagesServices;
const reviews: Review[] = reviewsList;
const itemsContact: ListItemContact[] = ItemsContact;
---

<Layout>
  <main>
    <section
      class="container mx-auto max-w-5xl px-2 md:px-5 py-10 lg:py-20"
      id="servicios"
    >
      <h2 class="mb-4 text-subheading">Nuestros Servicios</h2>
      <p class="text-body mb-4 text-primary">
        En <span class="font-semibold text-secondary">ERRE Recubrimientos</span> transformamos
        espacios en lugares duraderos, funcionales y estéticamente impecables. Somos
        especialistas en recubrimientos residenciales y comerciales, ofreciendo soluciones
        integrales que elevan la calidad y presentación de cada proyecto.
      </p>
      <div class="mt-10 space-y-5 md:space-y-0">
        {
          services.map((service: Service, index) => (
            <ServiceItem service={service} index={index} />
          ))
        }
      </div>
    </section>

    <section
      class="container mx-auto max-w-5xl px-2 md:px-5 py-10"
      id="trabajos"
    >
      <h2 class="text-subheading mb-4">Nuestros Trabajos Realizados</h2>
      <p class="mb-4 text-primary text-body">
        Cada proyecto refleja nuestro compromiso con la excelencia, la
        creatividad y la atencion al detalle. Descubre una seleccion de nuestros
        trabajos mas destacados.
      </p>
      <div>
        <div
          class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 auto-rows-[240px] grid-flow-row-dense gap-3"
        >
          {
            images.map((image: Image, index) => (
              <BentoImageItem image={image} index={index} />
            ))
          }
        </div>
      </div>
    </section>

    <section
      class="container mx-auto max-w-5xl px-2 md:px-5 py-10"
      id="testimonios"
    >
      <h2 class="text-subheading mb-4">Lo Que Dicen Nuestros Clientes</h2>
      <p class="mb-4 text-primary text-body">
        La satisfaccion de nuestros clientes es nuestro mejor indicador de
        exito. Estas son algunas de sus experiencias.
      </p>
      <div>
        <div
          class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 grid-flow-row-dense"
        >
          {reviews.map((review) => <ReviewCard review={review} />)}
        </div>
      </div>
    </section>

    <section class="bg-[#0d0d0d]" id="contacto">
      <div
        class="container mx-auto max-w-7xl px-5 py-20 lg:flex lg:items-center lg:justify-between"
      >
        <div class="flex-1">
          <h2 class="text-primary uppercase">Contacto</h2>
          <p class="text-white text-5xl my-3">Hablemos de tu proyecto</p>
          <p class="text-primary mt-10 w-[70%] max-w-lg">
            Completa el formulario y nos pondremos en contacto contigo para
            ofrecerte una cotización personalizada sin compromiso.
          </p>
          <div>
            <ul class="mt-10 space-y-8">
              {
                itemsContact.map((item: ListItemContact) => (
                  <ContactItem item={item} />
                ))
              }
            </ul>
          </div>
        </div>

        <form id="contactForm" class="space-y-10 mt-20 flex-1">
          <div class="space-y-2">
            <label
              for="name"
              class="block uppercase text-primary tracking-widest text-sm"
              >Nombre completo</label
            >
            <input
              type="text"
              name="name"
              id="name"
              required
              class="border-b border-primary w-full outline-0 p-2 text-white placeholder:text-primary"
              placeholder="Tu nombre"
              autocomplete="name"
            />
          </div>

          <div class="space-y-10 lg:flex lg:space-x-10 lg:space-y-0">
            <div class="space-y-2 lg:flex-1">
              <label
                for="email"
                class="block uppercase text-primary tracking-widest text-sm"
                >Correo electrónico</label
              >
              <input
                type="email"
                name="email"
                id="email"
                required
                class="border-b border-primary w-full outline-0 p-2 text-white placeholder:text-primary"
                placeholder="tu@email.com"
                autocomplete="email"
              />
            </div>

            <div class="space-y-2 lg:flex-1">
              <label
                for="phone"
                class="block uppercase text-primary tracking-widest text-sm"
                >Número de teléfono</label
              >
              <input
                type="tel"
                name="phone"
                id="phone"
                required
                class="border-b border-primary w-full outline-0 p-2 text-white placeholder:text-primary"
                placeholder="+52 1 234 567 8901"
                autocomplete="photo"
              />
            </div>
          </div>

          <div class="space-y-2">
            <label
              for="services"
              class="block uppercase text-primary tracking-widest text-sm"
              >Servicios</label
            >
            <select
              name="services"
              id="services"
              required
              class="border-b border-primary w-full outline-0 p-2 text-white bg-[#0d0d0d] cursor-pointer"
            >
              <option value="">Selecciona un servicio</option>
              <option value="Pintura residencial y comercial"
                >Pintura residencial y comercial</option
              >
              <option value="Impermeabilización">Impermeabilización</option>
              <option value="Aplicación de pasta">Mantenimiento</option>
              <option value="Aplicación de chukum">Aplicación de chukum</option>
              <option value="Servicios de electricidad"
                >Servicios de electricidad</option
              >
              <option value="Plomeria">Plomeria</option>
            </select>
          </div>

          <div class="space-y-2">
            <label
              for="message"
              class="block uppercase text-primary tracking-widest text-sm"
              >Mensaje</label
            >
            <textarea
              name="message"
              id="message"
              required
              class="border-b border-primary w-full outline-0 p-2 text-white placeholder:text-primary min-h-35"
              placeholder="Cuéntanos sobre tu proyecto..."></textarea>
          </div>
          <button
            type="submit"
            class="group inline-flex items-center gap-4 border border-primary text-white px-10 py-5 text-xs uppercase tracking-[0.15em] hover:bg-white hover:text-primary transition-all duration-500 cursor-pointer"
            >Enviar Mensaje</button
          >
        </form>
      </div>
    </section>
  </main>
</Layout>
